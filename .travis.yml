# this file is *not* meant to cover or endorse the use of travis, but rather to
# help confirm pull requests to this project.

language: python

matrix:
  include:
    - python: 3.4
      env: TOXENV=py34

install: pip install sofa

script: sofa

notifications:
email: false
